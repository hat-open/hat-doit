$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "hat-doit://tool.yaml"
type: object
properties:
    build_wheel_task:
        type: string
        default: build
    pytest_profile_dir:
        type: string
        default: 'build/profile'
